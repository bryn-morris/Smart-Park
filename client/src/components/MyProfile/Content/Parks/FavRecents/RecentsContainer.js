import FavArrow from "../Favorites/FavArrow"


function RecentsContainer () {

    // Determine Arrow logic and pass in arrowDirection props
    // will also need to create function, can likely pull out functions as helper function

    return(
        <div className = "recentsContainer">
            recentparks
        </div>
    )
}

export default RecentsContainer