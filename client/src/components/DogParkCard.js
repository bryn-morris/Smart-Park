

function DogParkCard({name, address, amenities, rating}){

    return (
    <div>
        <div>{name}</div>
        <div>{address}</div>
        <div>{amenities}</div>
        <div>{rating}</div>
    </div>
    )
}

export default DogParkCard

    